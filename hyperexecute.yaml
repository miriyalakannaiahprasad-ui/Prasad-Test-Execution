version: 0.1

globalTimeout: 1200  # 20 minutes

env:
  LT_USERNAME: miriyalakannaiahprasad
  LT_ACCESS_KEY: LT_zSJ1auyDTvYdGijmNUwJrx80QnIe02PahM54NTzZsq0I1U8

runson: linux

# Pre steps
pre:
  - git clone https://github.com/miriyalakannaiahprasad-ui/Prasad-Test-Execution.git
  - echo "Pre steps completed"

# Test scenarios (two separate scenarios)
test:
  - name: Run Amazon iPhone Test
    commands:
      - cd Prasad-Test-Execution && mvn -B -Dsurefire.useFile=false -Dtest=tests.AmazonIphoneTest test
      - echo "iPhone test completed"

  - name: Run Samsung Galaxy Test
    commands:
      - cd Prasad-Test-Execution && mvn -B -Dsurefire.useFile=false -Dtest=tests.GalaxyTest test
      - echo "Galaxy test completed"

# Post steps
post:
  - cd Prasad-Test-Execution && echo "Post steps executed"
  - cd Prasad-Test-Execution && mvn clean
